#!/usr/bin/python3
def weight_average(my_list=[]):
    if not my_list:
        return (0)
    scores = 0
    weights = 0
    for tup in my_list:
        scores += int_tuple[0] * int_tuple[1]
        weights += int_tuple[1]
    return (scores / weights)
